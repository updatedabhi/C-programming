#include <stdio.h>

int main()
{
    // reversing of array:
    
    /*
    int a[10];
    for (int i = 0; i < 10; i++)
    {
        a[i] = 5 * (i + 1);  
    }
    printf("Multiplicatio table of 5:\n");
    for (int i = 0; i < 10; i++)
    {
        printf("5 X %d = %d\n", (i + 1), a[i]);
    }
    
    int array[5];
    int *ptr = &array[0];
    ptr = ptr + 2;
    if (ptr == &array[2])
    {
        printf("How sweet");
    }
    else
    {
        printf("Not sweet");
    }
    */
    return 0;
}